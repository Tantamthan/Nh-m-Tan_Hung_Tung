@{
    ViewData["Title"] = ViewData["PageTitle"] ?? "Feature";
}

<section class="page-header-card">
    <span class="eyebrow">Lab 6 Secure Module</span>
    <h1>@ViewData["PageTitle"]</h1>
    <p class="lead">@ViewData["PageDescription"]</p>
</section>

<section class="content-card">
    <p>
        Trang này được tạo để đảm bảo navigation hoạt động đầy đủ theo cấu trúc menu yêu cầu của lab.
        Khi cần mở rộng nghiệp vụ thật, bạn chỉ việc thay action và view hiện tại.
    </p>
</section>
