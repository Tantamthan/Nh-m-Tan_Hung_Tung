@{
    ViewData["Title"] = "Privacy";
}

<section class="page-header-card">
    <span class="eyebrow">Security Notes</span>
    <h1>Privacy & Security</h1>
    <p class="lead">
        Ứng dụng áp dụng cookie authentication của ASP.NET Core Identity, role-based authorization,
        session lưu `CurrentUser`, cache menu bằng `IMemoryCache` và chống hiển thị lại trang bảo mật từ browser cache bằng `pageshow` reload.
    </p>
</section>
